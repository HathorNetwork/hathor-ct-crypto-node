# Rust build artifacts
target/
Cargo.lock

# Node
node_modules/

# Prebuilds (built by CI, not committed)
prebuilds/

# OS files
.DS_Store

# Local npm package build output (CI builds the real one on tag)
npm-package/
npm-package.zip
